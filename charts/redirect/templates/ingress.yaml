{{- range .Values.redirects }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{ .host | replace "." "-" }}"
  annotations:
    cert-manager.io/cluster-issuer: '{{ .issuer }}'
    traefik.ingress.kubernetes.io/router.entrypoints: {{ join "," $.Values.entryPoints }}
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .host }}"
spec:
  {{- with .Values.ingressClassName }}
  ingressClassName: {{ . | quote }}
  {{- end }}
  tls:
    - secretName: {{ default (printf "%s-tls" ((.host | quote ) | replace " " "-")) .tlsSecretName }}
      hosts:
        - {{ .host }}
  rules:
    - host: '{{ .host }}'
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name:  "{{ .host | replace "." "-" }}"
                port:
                  number: {{ .port }}
---
{{- end }}
